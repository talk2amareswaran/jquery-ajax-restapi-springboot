# jquery-ajax-restapi-springboot


Download the demo.rar and extract it.

Take a build by using command gradle clean build

Go to build/libs directory, run the jar file by using the command JAVA -JAR <jarfile>
